[project]
name = "component-gallery-ai-structured"
version = "0.1.0"
description = "Pydantic models for ComponentGalleryAI structured LLM outputs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7,<3"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
